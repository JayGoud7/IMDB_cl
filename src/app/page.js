import Cards from "../components/Cards/Cards";
import Sortsection from "../components/Cards/sort";

const page = () => {
  return (
    <section>
      <Cards />
    </section>
  );
};

export default page;
